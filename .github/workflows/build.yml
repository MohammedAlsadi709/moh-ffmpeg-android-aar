name: Build Ffmpeg Android (LGPL ONLY – FULL FIXED 4.4)

on:
  workflow_dispatch:

jobs:
  Build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install NDK r26
      run: |
        ANDROID_SDK_ROOT=/usr/local/lib/android/sdk
        mkdir -p $ANDROID_SDK_ROOT/ndk
        cd $ANDROID_SDK_ROOT/ndk
        wget https://dl.google.com/android/repository/android-ndk-r26d-linux.zip
        unzip -q android-ndk-r26d-linux.zip
        mv android-ndk-r26d r26

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y git build-essential yasm pkg-config nasm unzip wget curl tar autoconf automake libtool

    - name: Download Audio Dependencies
      run: |
        wget https://sourceforge.net/projects/lame/files/lame/3.100/lame-3.100.tar.gz
        tar -xzf lame-3.100.tar.gz
        wget https://downloads.xiph.org/releases/ogg/libogg-1.3.5.tar.gz
        tar -xzf libogg-1.3.5.tar.gz
        wget https://downloads.xiph.org/releases/vorbis/libvorbis-1.3.7.tar.gz
        tar -xzf libvorbis-1.3.7.tar.gz
        wget https://downloads.xiph.org/releases/opus/opus-1.4.tar.gz
        tar -xzf opus-1.4.tar.gz

    - name: Clone Ffmpeg
      run: |
        git clone https://github.com/ffmpeg/ffmpeg.git
        cd ffmpeg
        git checkout n4.4

    - name: Build Ffmpeg & External Libs
      run: |
        export NDK=/usr/local/lib/android/sdk/ndk/r26
        export TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64
        export API=21
        export AR=$TOOLCHAIN/bin/llvm-ar
        export NM=$TOOLCHAIN/bin/llvm-nm
        export STRIP=$TOOLCHAIN/bin/llvm-strip
        export RANLIB=$TOOLCHAIN/bin/llvm-ranlib

        EXTRA_CFLAGS="-fPIC"
        EXTRA_LDFLAGS="-Wl,-z,max-page-size=16384"
        
        WORKSPACE=$(pwd)
        PREFIX_ARM64=$WORKSPACE/prefix_arm64
        PREFIX_ARMV7=$WORKSPACE/prefix_armv7
        
        mkdir -p $PREFIX_ARM64
        mkdir -p $PREFIX_ARMV7

        # ================= arm64 =================
        export CC=$TOOLCHAIN/bin/aarch64-linux-android$API-clang
        export CXX=$TOOLCHAIN/bin/aarch64-linux-android$API-clang++
        export PKG_CONFIG_LIBDIR=$PREFIX_ARM64/lib/pkgconfig

        # Build LAME
        cd $WORKSPACE/lame-3.100
        ./configure --host=aarch64-linux-android --prefix=$PREFIX_ARM64 --disable-shared --enable-static --with-pic CC=$CC CXX=$CXX
        make -j$(nproc) && make install
        
        # Build OGG
        cd $WORKSPACE/libogg-1.3.5
        ./configure --host=aarch64-linux-android --prefix=$PREFIX_ARM64 --disable-shared --enable-static --with-pic CC=$CC CXX=$CXX
        make -j$(nproc) && make install
        
        # Build VORBIS (يستخدم مسار OGG من PKG_CONFIG_LIBDIR)
        cd $WORKSPACE/libvorbis-1.3.7
        ./configure --host=aarch64-linux-android --prefix=$PREFIX_ARM64 --disable-shared --enable-static --with-pic --with-ogg=$PREFIX_ARM64 CC=$CC CXX=$CXX LDFLAGS="-L$PREFIX_ARM64/lib" CPPFLAGS="-I$PREFIX_ARM64/include"
        make -j$(nproc) && make install
        
        # Build OPUS
        cd $WORKSPACE/opus-1.4
        ./configure --host=aarch64-linux-android --prefix=$PREFIX_ARM64 --disable-shared --enable-static --with-pic CC=$CC CXX=$CXX
        make -j$(nproc) && make install

        # Build Ffmpeg arm64
        cd $WORKSPACE/ffmpeg
        ./configure \
          --target-os=android --arch=aarch64 --cc=$CC --enable-cross-compile \
          --enable-shared --disable-static --disable-gpl --disable-nonfree \
          --disable-programs --disable-doc --disable-vulkan --disable-debug \
          --disable-avdevice --enable-libmp3lame --enable-libopus --enable-libvorbis \
          --extra-cflags="$EXTRA_CFLAGS -I$PREFIX_ARM64/include" \
          --extra-ldflags="$EXTRA_LDFLAGS -L$PREFIX_ARM64/lib" \
          --extra-libs="-lm"
        make -j$(nproc)
        mkdir -p output/arm64/lib
        cp libavcodec/*.so libavformat/*.so libavutil/*.so libswresample/*.so libswscale/*.so libavfilter/*.so output/arm64/lib/
        make clean

        # ================= armv7 =================
        export CC=$TOOLCHAIN/bin/armv7a-linux-androideabi$API-clang
        export CXX=$TOOLCHAIN/bin/armv7a-linux-androideabi$API-clang++
        export PKG_CONFIG_LIBDIR=$PREFIX_ARMV7/lib/pkgconfig

        cd $WORKSPACE/lame-3.100 && make clean && ./configure --host=arm-linux-androideabi --prefix=$PREFIX_ARMV7 --disable-shared --enable-static --with-pic CC=$CC CXX=$CXX && make -j$(nproc) && make install
        cd $WORKSPACE/libogg-1.3.5 && make clean && ./configure --host=arm-linux-androideabi --prefix=$PREFIX_ARMV7 --disable-shared --enable-static --with-pic CC=$CC CXX=$CXX && make -j$(nproc) && make install
        cd $WORKSPACE/libvorbis-1.3.7 && make clean && ./configure --host=arm-linux-androideabi --prefix=$PREFIX_ARMV7 --disable-shared --enable-static --with-pic --with-ogg=$PREFIX_ARMV7 CC=$CC CXX=$CXX LDFLAGS="-L$PREFIX_ARMV7/lib" CPPFLAGS="-I$PREFIX_ARMV7/include" && make -j$(nproc) && make install
        cd $WORKSPACE/opus-1.4 && make clean && ./configure --host=arm-linux-androideabi --prefix=$PREFIX_ARMV7 --disable-shared --enable-static --with-pic CC=$CC CXX=$CXX && make -j$(nproc) && make install

        cd $WORKSPACE/ffmpeg
        ./configure \
          --target-os=android --arch=arm --cc=$CC --enable-cross-compile \
          --enable-shared --disable-static --disable-gpl --disable-nonfree \
          --disable-programs --disable-doc --disable-vulkan --disable-debug \
          --disable-avdevice --enable-libmp3lame --enable-libopus --enable-libvorbis \
          --extra-cflags="$EXTRA_CFLAGS -I$PREFIX_ARMV7/include" \
          --extra-ldflags="$EXTRA_LDFLAGS -L$PREFIX_ARMV7/lib" \
          --extra-libs="-lm"
        make -j$(nproc)
        mkdir -p output/armv7/lib
        cp libavcodec/*.so libavformat/*.so libavutil/*.so libswresample/*.so libswscale/*.so libavfilter/*.so output/armv7/lib/

    - name: Export Ffmpeg Full Kit
      run: |
        cd ffmpeg
        mkdir -p export/include export/fftools export/compat
        cp -r libavcodec libavformat libavutil libswresample libswscale libavfilter export/include/
        cp config.h export/include/ || cp ffbuild/config.h export/include/
        cp -r compat/* export/compat/
        # في 4.4 نكتفي بهذه الملفات الأربعة الأساسية لـ fftools
        cp fftools/ffmpeg.c fftools/cmdutils.c fftools/ffmpeg_opt.c fftools/ffmpeg_filter.c fftools/*.h export/fftools/

    - name: Upload Ffmpeg Full Kit
      uses: actions/upload-artifact@v4
      with:
        name: ffmpeg-4.4-fixed
        path: |
          ffmpeg/output/arm64/lib/*.so
          ffmpeg/output/armv7/lib/*.so
          ffmpeg/export/**/*
          
